# Changesets

Drop a `.md` file here when you make a change that affects published packages (`@junui/ui`, `@junui/tokens`, `@junui/utils`).

```bash
pnpm changeset
```

Will walk you through the process. Apps and Phase 3~5 internal packages are excluded from publishing in `config.json`.
